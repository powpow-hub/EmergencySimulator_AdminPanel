<Window x:Class="EmergencySimulator.AdminPanel.Views.SelectStaffWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Выбор сотрудника для ВГК" 
        Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="10" 
            BorderThickness="1" 
            BorderBrush="#DDDDDD">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Direction="270" 
                            ShadowDepth="5" 
                            BlurRadius="15" 
                            Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" 
                    Background="#2E3A47" 
                    CornerRadius="10,10,0,0" 
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="60" Margin="20,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="👥 ВЫБОР СОТРУДНИКА" 
                                 Foreground="White" 
                                 FontSize="16" 
                                 FontWeight="Bold"/>
                        <TextBlock Text="Добавление члена в состав ВГК" 
                                 Foreground="#B0BEC5" 
                                 FontSize="12"
                                 Margin="0,5,0,0"/>
                    </StackPanel>

                    <Button Content="✕" 
                          Width="40" Height="40"
                          Background="Transparent"
                          Foreground="#666666"
                          BorderThickness="0"
                          FontSize="16"
                          FontWeight="Bold"
                          Cursor="Hand"
                          HorizontalAlignment="Right"
                          Click="CancelButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Поиск -->
            <Border Grid.Row="1" Padding="20,15,20,0">
                <TextBox x:Name="SearchTextBox"
                       FontSize="14"
                       Padding="10"
                       TextChanged="SearchTextBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 Поиск по ФИО, должности..." 
                                                             Foreground="#999999" 
                                                             Margin="5,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Border>

            <!-- Список сотрудников -->
            <Border Grid.Row="2" Margin="20,15,20,20">
                <ListBox x:Name="StaffListBox"
                       SelectionMode="Single"
                       BorderBrush="#DDDDDD"
                       BorderThickness="1"
                       MouseDoubleClick="StaffListBox_MouseDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10" 
                                  Background="Transparent"
                                  BorderBrush="#E0E0E0"
                                  BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontSize="15" FontWeight="SemiBold">
                                            <Run Text="{Binding Surname}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding Name}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding MiddleName}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Position}" 
                                                 FontSize="12" 
                                                 Foreground="#666666"
                                                 Margin="0,3,0,0"/>
                                        <TextBlock Text="{Binding Phone}" 
                                                 FontSize="11" 
                                                 Foreground="#999999"
                                                 Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <Border Background="#E8F5E9"
                                              Padding="8,4"
                                              CornerRadius="3"
                                              Margin="0,0,0,3">
                                            <TextBlock Text="Активен" 
                                                     FontSize="10"
                                                     FontWeight="Bold"
                                                     Foreground="#4CAF50"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Кнопки -->
            <Border Grid.Row="3" 
                    Background="#F5F5F5" 
                    BorderBrush="#DDDDDD" 
                    BorderThickness="0,1,0,0" 
                    CornerRadius="0,0,10,10"
                    Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="✔ Выбрать" 
                          Style="{StaticResource ButtonStyle}"
                          Click="SelectButton_Click"/>

                    <Button Content="✖ Отмена" 
                          Click="CancelButton_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
                                <Setter Property="Background" Value="#757575"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#616161"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
