<UserControl x:Class="EmergencySimulator.AdminPanel.Views.PLAView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:EmergencySimulator.AdminPanel.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100">

    <UserControl.Resources>
        <!-- Конвертеры -->
        <converters:CriticalToBoldConverter x:Key="CriticalToBold"/>
        <converters:CriticalToColorConverter x:Key="CriticalToColor"/>
        <converters:SecondsToMinutesConverter x:Key="SecondsToMinutes"/>
        <converters:BooleanToYesNoConverter x:Key="BoolToYesNo"/>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Информационная панель -->
        <Border Grid.Row="1" Background="#FFF3E0" BorderBrush="#FF9800" BorderThickness="1" 
                CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock FontWeight="Bold" Foreground="#E65100">
                    <Run Text="ℹ️ Справка:"/>
                </TextBlock>
                <TextBlock TextWrapping="Wrap" Foreground="#5D4037" Margin="0,5,0,0">
                    <Run Text="• "/>
                    <Run Text="Позиции" FontWeight="Bold"/>
                    <Run Text=" — типы аварийных ситуаций (№3 — Пожар, №7 — Обрушение и т.д.)"/>
                    <LineBreak/>
                    <Run Text="• "/>
                    <Run Text="Действия" FontWeight="Bold"/>
                    <Run Text=" — последовательные шаги для ликвидации аварии"/>
                    <LineBreak/>
                    <Run Text="• "/>
                    <Run Text="Критичные действия" FontWeight="Bold"/>
                    <Run Text=" отмечены красным — их невыполнение приведёт к провалу тренировки"/>
                    <LineBreak/>
                    <Run Text="• "/>
                    <Run Text="Норматив времени" FontWeight="Bold"/>
                    <Run Text=" — максимально допустимое время выполнения действия (в секундах)"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Панель поиска -->
        <Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" 
                CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14" Padding="10" VerticalContentAlignment="Center"
                         BorderBrush="#CCCCCC" BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 Поиск по позиции, названию или действию..." 
                                                             Foreground="#999999" Margin="5,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Column="1" Content="🔄 Обновить" 
                        Style="{StaticResource ButtonStyle}" 
                        Command="{Binding RefreshCommand}"/>
            </Grid>
        </Border>

        <!-- Таблица данных ПЛА -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding PLADataList}" SelectedItem="{Binding SelectedPLA}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" IsReadOnly="False" GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9F9F9" BorderBrush="#DDDDDD" BorderThickness="1" 
                  RowHeight="60" EnableRowVirtualization="True">

            <DataGrid.Columns>
                <!-- ID -->
                <DataGridTextColumn Header="ID" Binding="{Binding PlaID}" Width="50" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#666666"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Позиция -->
                <DataGridTextColumn Header="Позиция" Binding="{Binding PositionNumber}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Название позиции -->
                <DataGridTextColumn Header="Название" Binding="{Binding PositionName}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Действие с использованием конвертера для критичных -->
                <DataGridTextColumn Header="Действие" Binding="{Binding ActionStep}" Width="350">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="Foreground" Value="{Binding IsCritical, Converter={StaticResource CriticalToColor}}"/>
                            <Setter Property="FontWeight" Value="{Binding IsCritical, Converter={StaticResource CriticalToBold}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Порядок -->
                <DataGridTextColumn Header="Порядок" Binding="{Binding StepOrder}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="Foreground" Value="#2196F3"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Норматив времени с конвертером -->
                <DataGridTextColumn Header="Норматив" Width="90">
                    <DataGridTextColumn.Binding>
                        <Binding Path="TimeNormative" Converter="{StaticResource SecondsToMinutes}"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Foreground" Value="#FF6F00"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Критично с конвертером -->
                <DataGridTextColumn Header="Критично" Width="85" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsCritical" Converter="{StaticResource BoolToYesNo}"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="{Binding IsCritical, Converter={StaticResource CriticalToColor}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Описание -->
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*" MinWidth="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="Foreground" Value="#666666"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2E3A47"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    <Setter Property="BorderBrush" Value="#3A4A5A"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCritical}" Value="True">
                            <Setter Property="Background" Value="#FFEBEE"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Панель управления -->
        <Border Grid.Row="4" Background="White" BorderBrush="#DDDDDD" 
                BorderThickness="1,1,1,0" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="➕ Добавить действие" 
                        Style="{StaticResource ButtonStyle}" 
                        Command="{Binding AddPLACommand}"/>

                <Button Content="✏️ Сохранить изменения" 
                        Style="{StaticResource ButtonStyle}" 
                        Command="{Binding EditPLACommand}"/>

                <Button Content="⚠️ Критичность" 
                ToolTip="Переключить критичность выбранного действия"
                Command="{Binding ToggleCriticalCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
                            <Setter Property="Background" Value="#FF9800"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F57C00"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="❌ Удалить" 
                        Command="{Binding DeletePLACommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
                            <Setter Property="Background" Value="#D13438"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#B12A2E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>