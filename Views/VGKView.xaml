<UserControl x:Class="EmergencySimulator.AdminPanel.Views.VGKView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:EmergencySimulator.AdminPanel.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100">

    <UserControl.Resources>
        <!-- Конвертеры -->
        <converters:BooleanToOnDutyConverter x:Key="BoolToOnDuty"/>
        <converters:BooleanToYesNoConverter x:Key="BoolToYesNo"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibility"/>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InfoCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="BorderBrush" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель статистики -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Border Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="Всего членов ВГК" FontSize="12" Foreground="#666666"/>
                    <TextBlock Text="{Binding TotalMembers}" FontSize="24" FontWeight="Bold" Foreground="#2196F3"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="На дежурстве" FontSize="12" Foreground="#666666"/>
                    <TextBlock Text="{Binding OnDutyCount}" FontSize="24" FontWeight="Bold" Foreground="#4CAF50"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="Командиров" FontSize="12" Foreground="#666666"/>
                    <TextBlock Text="{Binding CommanderCount}" FontSize="24" FontWeight="Bold" Foreground="#FF9800"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Панель поиска и фильтров -->
        <Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Строка поиска -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="14" Padding="10" VerticalContentAlignment="Center"
                             BorderBrush="#CCCCCC" BorderThickness="1">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="🔍 Поиск по ФИО или роли..." Foreground="#999999" Margin="5,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <Button Grid.Column="1" Content="🔄 Обновить" Style="{StaticResource ButtonStyle}" Command="{Binding RefreshCommand}"/>
                    <Button Grid.Column="2" Content="🗑️ Очистить фильтры" Style="{StaticResource ButtonStyle}" Command="{Binding ClearFiltersCommand}"/>
                </Grid>

                <!-- Фильтры -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Роль:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding AvailableRoles}" 
                              SelectedItem="{Binding FilterRole}"
                              Width="200" 
                              Padding="8"
                              Margin="0,0,20,0">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <CheckBox Content="Только на дежурстве" 
                              IsChecked="{Binding ShowOnDutyOnly}"
                              VerticalAlignment="Center"
                              FontWeight="SemiBold"
                              Margin="0,0,20,0"/>

                    <TextBlock Text="{Binding TotalMembers, StringFormat='Найдено: {0}'}" 
                               VerticalAlignment="Center"
                               Foreground="#2196F3"
                               FontWeight="Bold"
                               Margin="20,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Таблица членов ВГК -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding VGKMembers}" SelectedItem="{Binding SelectedMember}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" IsReadOnly="False" GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9F9F9" BorderBrush="#DDDDDD" BorderThickness="1" RowHeight="45">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding MemberID}" Width="60" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Фамилия" Binding="{Binding Staff.Surname}" Width="150" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Имя" Binding="{Binding Staff.Name}" Width="130" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Отчество" Binding="{Binding Staff.MiddleName}" Width="150" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Должность" Binding="{Binding Staff.Position}" Width="180" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Роль в ВГК" Binding="{Binding Role}" Width="150"/>

                <!-- Командир с конвертером -->
                <DataGridTextColumn Header="Командир" Width="90" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsCommander" Converter="{StaticResource BoolToYesNo}"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- На дежурстве с конвертером -->
                <DataGridTextColumn Header="Статус" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsOnDuty" Converter="{StaticResource BoolToOnDuty}"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2E3A47"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCommander}" Value="True">
                            <Setter Property="Background" Value="#FFF9C4"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsOnDuty}" Value="False">
                            <Setter Property="Foreground" Value="#999999"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Панель управления -->
        <Border Grid.Row="4" Background="White" BorderBrush="#DDDDDD" BorderThickness="1,1,1,0" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="➕ Добавить" Style="{StaticResource ButtonStyle}" Command="{Binding AddMemberCommand}"/>
                <Button Content="✏️ Сохранить" Style="{StaticResource ButtonStyle}" Command="{Binding EditMemberCommand}"/>
                <Button Content="👤 Назначить командиром" Style="{StaticResource ButtonStyle}" Command="{Binding SetAsCommanderCommand}"/>
                <Button Content="🔄 Дежурство" Style="{StaticResource ButtonStyle}" Command="{Binding ToggleDutyCommand}"/>
                <Button Content="📋 Детали" Style="{StaticResource ButtonStyle}" Command="{Binding ViewMemberDetailsCommand}"/>
                <Button Content="❌ Удалить" Command="{Binding DeleteMemberCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
                            <Setter Property="Background" Value="#D13438"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#B12A2E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
